<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.ukonnect.MyClubList">

   <EditText
       android:id="@+id/Club_Name_Filter"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:iconifiedByDefault="false"
       android:hint="Filter Clubs(NEED TO IMPLEMENT)"/>
   <Button
       android:id="@+id/list_AddClub"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:background="#E0F2F1"
       android:text="Browse Clubs(MAY CRASH IF OPENED TOO MANY TIMES, DOSNT CLOSE OLD ACTIVITIES I THINK, USE WITH CAUTION)(MAY FIX LATER OR JUST REMOVE IF LAZY)"
       android:drawableLeft="@drawable/ic_add_black_24dp"
       android:paddingLeft="5dp"
       android:onClick="toAddNew"/>


   <ScrollView
    android:id="@+id/Club_List"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    android:fillViewport="true">

      <LinearLayout
          android:id="@+id/Club_List_linear"
          android:layout_width="match_parent"
          android:layout_height="match_parent"
          android:layout_margin="3dp"
          android:orientation="vertical"/>

       </ScrollView>

</LinearLayout>